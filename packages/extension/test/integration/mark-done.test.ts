// Placeholder for flow #4 (Mark as Done). Real assertions added in a
// follow-up commit; this stub keeps the compile script honest.
suite('Integration: mark done', () => {
  test.skip('placeholder', () => {});
});
